{
  "rule_name": "格式化规则",
  "description": "关于文档格式、布局和强调方式的规则",
  "priority": "中",
  "layout_rules": {
    "rendering_width": {
      "description": "文章在网页上的渲染宽度可以放置 80 个中文字符",
      "requirement": "需要保证文章在渲染时不要太空"
    },
    "title_rules": {
      "max_heading_level": 3,
      "no_toc": "文章不需要添加目录，目录在网站上会自动生成"
    }
  },
  "emphasis_rules": {
    "highlight": {
      "description": "重点要突出显示",
      "allowed_patterns": ["`entry`", "**entry**"],
      "disallowed_patterns": ["**`entry`**", "**1`entry`**", "**`entry`2**"]
    }
  },
  "readability_elements": {
    "description": "使用表格、分割线、图标等元素，使文档更具可读性",
    "additional_note": "适当使用列表组织内容"
  },
  "examples": {
    "correct_emphasis": [
      "这是一个`重要概念`",
      "这是一个**重要概念**"
    ],
    "incorrect_emphasis": [
      "这是一个**`重要概念`**",
      "这是一个**1`重要概念`**"
    ]
  }
}