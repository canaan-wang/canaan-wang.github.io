# ForkJoinPool 并行分治

- 用途：适合可分治的计算任务（递归拆分），充分利用多核并行。
- 核心概念：
  - 工作窃取（Work-Stealing）：空闲线程从其他线程的双端队列窃取任务，提升负载均衡。
  - 任务类型：RecursiveTask（有返回）、RecursiveAction（无返回）。
- 示例思路：递归拆分到阈值后直接计算，否则继续拆分并合并结果。
- 注意：
  - 任务粒度要合适，避免过细导致调度开销过大。
  - 与公共池（commonPool）共用时，避免长时间阻塞任务影响其他异步任务。